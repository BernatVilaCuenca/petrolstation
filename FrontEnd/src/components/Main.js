import React, { Component } from 'react';
import Menu from './Menu';

export default class Main extends Component {  
  render() {
    return (
      <div>
          <Menu/>
      </div>
    );
  }
};