import Styled from 'styled-components';

const data = {
    buttons: {
        image: Styled.span`
            margin-right: 10px; 
            cursor: pointer;
        `
    },
    table: Styled.div`float:left; width: 80%`
};
export const styles = data;